document.addEventListener('DOMContentLoaded', () => {
  const tabSearch = document.getElementById('tabSearch');
  if (tabSearch) {
    tabSearch.addEventListener('click', () => {
      console.log('Tab Search clicked');
      // Add your search functionality here
    });
  }     
});